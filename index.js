// Write your code in this file!
// Define the currentUser variable
const currentUser = "Jonathan"; // Replace with any name

// Create welcomeMessage using currentUser
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

// Create excitedWelcomeMessage (uppercase)
const excitedWelcomeMessage = `WELCOME TO FLATBOOK, ${currentUser.toUpperCase()}!`;

// Create shortGreeting using only the first letter of currentUser
const shortGreeting = `Welcome, ${currentUser[0]}!`;
